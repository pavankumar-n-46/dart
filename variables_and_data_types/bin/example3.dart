void main(List<String> args) {
  var address = "123, address street";
  print(address);

  address = "hello there!";
  print(address);

  /*
  var will allow mutation of both the memory address of the element as well as the content stored in that address
  */
}
